# Cleanup Report — Files Safe to Remove Later

**Created:** 2026-07-05  
**Context:** After the Android port is fully functional and the web-only features are confirmed unnecessary, the following upstream assets can be considered for removal.

> **Do NOT delete these yet.** Only remove after confirming the Android port works end-to-end and these files are not referenced anywhere.

---

## Upstream Images (Not Used by Android Branding)

| File | Reason | Safe to Remove? |
|------|--------|-----------------|
| `public/social_preview_index.jpg` | Upstream social preview — not used in Android README or branding | ✅ After confirming no code reference |
| `public/logo-dark.png` | Upstream bolt.diy logo — replaced by `bolt-diy-android-logo.svg` | ⚠️ Check if UI still references it |
| `public/logo-dark-styled.png` | Upstream bolt.diy logo variant | ⚠️ Check if UI still references it |
| `public/logo-light.png` | Upstream bolt.diy logo | ⚠️ Check if UI still references it |
| `public/logo-light-styled.png` | Upstream bolt.diy logo variant | ⚠️ Check if UI still references it |
| `public/apple-touch-icon-precomposed.png` | Upstream Apple touch icon — not needed for Android | ✅ After confirming |
| `public/apple-touch-icon.png` | Upstream Apple touch icon | ✅ After confirming |

## Upstream Logos Referenced in Code

The following files may reference upstream logos in the UI. Before removing logo files, search and replace references:

```
grep -rn "logo-dark\|logo-light\|logo\.svg" app/ --include="*.tsx" --include="*.ts"
```

Replace with `bolt-diy-android-logo.svg` where appropriate.

## Web-Only Features (Potential Future Removal)

| Component | Reason | Safe to Remove? |
|-----------|--------|-----------------|
| `app/lib/webcontainer/` | WebContainer not available on Android | ❌ Keep — used on web/desktop |
| `app/lib/adapters/webcontainer-adapter.ts` | WebContainer adapter | ❌ Keep — used on web/desktop |
| `app/components/workbench/Preview.tsx` | Live preview | ❌ Keep — has CSS fallback |

> **Note:** The web/desktop version still uses these. They should only be removed if this fork becomes Android-only.

## Build Artifacts

| Path | Reason | Safe to Remove? |
|------|--------|-----------------|
| `build/` | Build output — regenerated on each build | ✅ Safe to remove anytime |
| `android/app/src/main/assets/public/` | Old synced web assets | ✅ Regenerated by `cap sync` |

## Old Java Package Path

| Path | Status |
|------|--------|
| `android/app/src/main/java/com/boltdiy/` | ✅ Already removed in branding pass |

---

## Recommended Cleanup Order

1. **After Android port works end-to-end:**
   - Remove `public/social_preview_index.jpg` (not referenced)
   - Remove `public/apple-touch-icon*.png` (iOS only)

2. **After replacing logo references in UI:**
   - Remove `public/logo-dark*.png`, `public/logo-light*.png`
   - Keep `public/logo.svg` until all references replaced

3. **After confirming no web-only deployment:**
   - Consider removing WebContainer-related code
   - Keep adapters as they provide the abstraction layer
